

---

```markdown
# 📡 UART (Universal Asynchronous Receiver/Transmitter)

## 1. UART란?
- **Universal Asynchronous Receiver/Transmitter**
- **범용 비동기 송수신 장치**  
- **시리얼 통신** 방식 중 하나로, 데이터를 **1비트씩 순서대로** 전송
- 보드(마이크로컨트롤러)와 컴퓨터, 센서 등 **두 장치 간의 데이터 송수신**을 담당하는 하드웨어 모듈

---

## 2. 동작 원리
UART는 **병렬 데이터 ↔ 직렬 데이터** 변환을 수행합니다.

1. **송신(Tx)**  
   - CPU나 메모리에서 오는 **병렬 데이터**를  
     → 1비트씩 시간 순서대로 직렬 데이터로 변환  
   - 변환된 데이터를 **Tx 핀**으로 전송

2. **수신(Rx)**  
   - Rx 핀으로 들어오는 직렬 데이터를  
     → 병렬 데이터로 변환하여 CPU에 전달

---

## 3. 특징
- **비동기(Asynchronous)**  
  - 송신측과 수신측이 **동일한 속도(보드레이트)**만 맞추면 통신 가능  
  - 별도의 클럭 신호선이 없음
- **Full-Duplex 지원** (동시에 송수신 가능)
- 하드웨어 구조가 단순하여 MCU, PC, 임베디드 장비에서 많이 사용

---

## 4. 기본 연결 방식
UART는 최소 **2개의 신호선**으로 동작합니다.

| 핀 | 역할 |
|----|------|
| **Tx (Transmit)** | 데이터 전송 |
| **Rx (Receive)** | 데이터 수신 |
| (선택) GND | 기준 전압 공유 |

💡 **교차 연결 필수**  
- 장치 A의 Tx → 장치 B의 Rx  
- 장치 A의 Rx → 장치 B의 Tx

---

## 5. 데이터 프레임 구조
하나의 UART 전송 데이터는 다음과 같이 구성됩니다.

```

| Start bit | Data bits | Parity bit(옵션) | Stop bit |

```

- **Start bit** : 통신 시작을 알림 (항상 `0`)
- **Data bits** : 전송할 실제 데이터 (보통 7~8비트)
- **Parity bit** : 오류 검출용 (짝수/홀수 패리티)
- **Stop bit** : 통신 종료 표시 (`1` 유지)

---

## 6. 주요 설정값
| 설정 항목 | 설명 |
|-----------|------|
| **Baud Rate** | 초당 전송 비트 수 (예: 9600, 115200bps) |
| **Data Bits** | 전송 데이터 비트 수 |
| **Parity** | 오류 검출 방식 (None, Even, Odd) |
| **Stop Bits** | 데이터 전송 종료 표시 비트 개수 (1, 1.5, 2) |

---

## 7. UART vs USB/RS-232/RS-485
| 구분 | UART | RS-232 | RS-485 | USB |
|------|------|--------|--------|-----|
| 통신 방식 | 비동기 직렬 | 비동기 직렬 | 비동기 직렬 | 패킷 기반 |
| 전송 거리 | 짧음(수 m) | 수십 m | 수백 m | 수 m |
| 배선 수 | 2~3 | 3 이상 | 2 | 4 |
| 특징 | 단순, MCU 내장 | 오래된 표준 | 장거리, 다중노드 | 고속, 범용 |

---

## 8. 장점 & 단점
**장점**
- 하드웨어/펌웨어 구현이 간단
- 저전력 소모
- 짧은 거리에서 안정적

**단점**
- 전송 거리 제한
- 다중 장치 연결이 어려움 (1:1 구조)
- 클럭이 없어 장거리에서 속도 오차 발생 가능

---

## 9. 사용 예시
- 아두이노 ↔ 센서 모듈
- 라즈베리파이 ↔ GPS 모듈
- MCU ↔ PC 시리얼 모니터
- 블루투스 모듈(HC-05, HC-06)과 MCU 통신

---

## 10. 시각 예시

```

[장치 A] Tx -----> Rx [장치 B]  
[장치 A] Rx <----- Tx [장치 B]

```

![UART Connection Diagram](https://upload.wikimedia.org/wikipedia/commons/9/9b/Serial_port_signals.svg)
```

---

이렇게 쓰면 장치관리자의 **COM 포트**가 UART 통신을 통해서 동작하는 경우를 이해하기 훨씬 쉽게 보실 수 있을 거예요.  
원하시면 제가 **"COM 포트 = PC에서 UART 장치 인식 과정"**을 그림으로 만들어서 바로 보여드릴 수 있습니다.
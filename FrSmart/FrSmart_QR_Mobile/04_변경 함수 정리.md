

---

# 1) `setUiFindExpDlg(self, number)`가 하는 일 한눈 요약

- 상세 화면 배경 이미지를 세팅하고
    
- 현재 선택된 옵션들(성별/나이/MBTI/패션/색상/목적/향 카테고리 등)로 **결과 페이지 URL 쿼리스트링**을 만들고
    
- 그 URL로 **QR 코드를 생성**해서
    
- 다이얼로그 오른쪽 위에 **QLabel**로 보여줍니다.
    

---

## 1-A) 흐름도

배경이미지 세팅 → 선택값 수집/정리 → URL 파라미터 딕셔너리 구성 → `dsQRcode.generate_qr_code(...)` 호출 → PNG 저장 → `QLabel`에 Pixmap으로 표시

---

## 1-B) 코드 라인-바이-라인 설명

```python
def setUiFindExpDlg(self, number):
```

- `number`: 결과 페이지의 `perfume` 파라미터로 들어가는 인덱스(또는 ID)입니다.
    

```python
self.ui_find_exp_dlg.label_background.setStyleSheet(
    dsImage.dsFindDetailImg[number])
```

- 다이얼로그 배경에 사용할 CSS 문자열을 **리스트/딕셔너리에서 인덱스로 꺼내** 적용합니다.
    
- `number`가 유효한 범위인지(인덱스 에러 방지)만 주의하세요.
    

```python
try:
    mbti_str = self._convert_personality_to_mbti(getattr(self, 'find_per_range_num', 0))
```

- 내부에 저장해 둔 “성향 선택값(`find_per_range_num`)"을 **MBTI 문자열**로 바꿉니다.
    
- 속성이 없으면 기본값 0. 변환 함수가 0일 때 `'NONE'`을 주는 설계로 보이네요.
    

```python
def nz(x): 
    return int(x) if isinstance(x, (int, float)) else int(x) if str(x).isdigit() else 0
```

- “숫자처럼 보이면 int로, 아니면 0”으로 정리하는 **보정 함수**예요.
    
- 장점: `None`, 빈 문자열, 이상한 값이 들어와도 0으로 안전하게 떨어짐.
    
- 주의: `'1.0'`, `' -1'`(공백/부호 포함) 같은 문자열은 `str.isdigit()`이 False라 0이 됩니다. 아래 “개선 팁”에 더 안전한 버전 제안 드릴게요.
    

```python
params = {
    "perfume": number,
    "gender_id": nz(getattr(self, 'find_gender_range_num', 0)),
    "age_id": nz(getattr(self, 'find_age_range_num', 0)),
    "mbti": mbti_str if mbti_str != 'NONE' else "",
    "fashion_id": nz(getattr(self, 'find_fashion_range_num', 0)),
    "pref_color_id": nz(getattr(self, 'find_color_range_num', 0)),
    "purpose_id": nz(getattr(self, 'find_purpose_range_num', 0)),
```

- 결과 페이지가 이해하는 **쿼리 키 이름**으로 현재 선택값을 담습니다.
    
- `mbti`는 `'NONE'`이면 빈 문자열로 두고, 나머지는 숫자화(`nz`) 하여 정수로 만듭니다.
    

```python
    # 🔧 여기! Scent(=카테고리) 선택값을 실제로 쓰기
    # 기존: "category_id": nz(getattr(self, 'find_category_range_num', 0)),
    "category_id": nz(getattr(self, 'find_scent_range_num', getattr(self, 'find_category_range_num', 0))),
```

- 핵심! **향 카테고리**는 `find_scent_range_num`이 있으면 그 값을, 없으면 구(舊) 필드 `find_category_range_num`을 씁니다(후행 호환).
    
- 이렇게 하면 UI가 “Scent”로 바뀐 뒤에도 **URL 파라미터에 제대로 반영**됩니다.
    

```python
    "top": 30, "middle": 40, "base": 30,
    "lang": "en",
}
```

- 결과 페이지에서 **노트 비율**과 **언어**를 이해한다는 전제. 없으면 프론트에서 기본값 처리해도 됩니다.
    

```python
qr_path = dsQRcode.generate_qr_code(
    save_path=f"temp_qr_{number}.png",
    base_url="https://perfume-mobile.vercel.app/result",
    params=params,
)
```

- 실제 QR 이미지를 **파일로 저장**하고, 경로를 돌려받습니다.
    
- `dsQRcode.generate_qr_code`는 `params`를 **URL 인코딩**해서 `base_url?...` 형태의 최종 URL을 만들어요.
    

```python
if not hasattr(self, 'qr_label'):
    self.qr_label = QtWidgets.QLabel(self.ui_find_exp_dlg)
    self.qr_label.setFixedSize(100, 100)
    self.qr_label.setStyleSheet("border: none; background: transparent;")
```

- 다이얼로그 안에 **QR 표시용 QLabel**을 **1회만 생성**(재사용)합니다.
    

```python
qr_pixmap = QtGui.QPixmap(qr_path)
self.qr_label.setPixmap(qr_pixmap.scaled(100, 100, Qt.AspectRatioMode.KeepAspectRatio))
self.qr_label.move(self.ui_find_exp_dlg.width() - 120, 20)
self.qr_label.show()
```

- PNG → Pixmap으로 읽고, **비율 유지**로 100×100에 맞춰 스케일링.
    
- 다이얼로그 오른쪽 위(좌측 여백 120px, 상단 20px)에 배치합니다.
    
- **주의**: `Qt.AspectRatioMode...`를 쓰려면 `from PySide6.QtCore import Qt`가 필요합니다.  
    (아니면 `QtCore.Qt.AspectRatioMode...`로 쓰세요.)
    

```python
except Exception as e:
    print(f"QR 코드 생성 오류: {e}")
    if hasattr(self, 'qr_label'):
        self.qr_label.hide()
```

- QR 생성 실패 시 콘솔에 원인 출력하고 라벨 숨깁니다.
    

---

# 2) `dsQRcode.py`가 하는 일

## 2-A) 상단 상수

```python
LEGACY_BASE_URL = "https://hashswim.github.io/perfume_mobile_page/"
DEFAULT_RESULT_URL = "https://perfume-mobile.vercel.app/result"
```

- **레거시 URL**(idx 기반)과 **새 결과 페이지 URL**을 분리.
    
- 기본은 새 결과 페이지로 갑니다.
    

## 2-B) `generate_qr_code(...)` 동작

```python
def generate_qr_code(idx=None, save_path=None, *, base_url=DEFAULT_RESULT_URL, params=None):
```

- `params`가 주어지면 그걸 **쿼리스트링으로 인코딩**해서 `base_url` 뒤에 붙이고,
    
- `params`가 없으면 (과거 호환) **`idx`만 포함하는 레거시 URL**을 씁니다.
    

```python
if params and isinstance(params, dict):
    cleaned = {k: v for k, v in params.items() if v not in (None, "", -1)}
    full_url = f"{base_url}?{urlencode(cleaned)}"
else:
    if idx is None:
        idx = random.randint(0, 11)
    full_url = f"{LEGACY_BASE_URL}?{urlencode({'idx': idx})}"
```

- **빈 값( None, 빈 문자열, -1 )은 제거**해 URL을 최대한 간결히 만듭니다.
    
- `urlencode`가 자동으로 `&`와 이스케이프 처리를 해 줍니다.
    

```python
qr = qrcode.QRCode(
    version=1,
    error_correction=qrcode.constants.ERROR_CORRECT_L,
    box_size=10,
    border=4,
)
qr.add_data(full_url)
qr.make(fit=True)
img = qr.make_image(fill_color="black", back_color="white")
```

- qrcode 라이브러리로 **QR 비트맵**을 생성.
    
- `fit=True`라 데이터 길이에 맞게 **버전(크기) 자동 조정**이 됩니다.  
    (`version=1`은 시작점 역할. 길이가 길면 크게 올라갑니다.)
    
- `ERROR_CORRECT_L`는 오류 복원력이 가장 낮은(=용량 효율 좋은) 등급입니다.  
    인쇄/카메라 인식 신뢰도를 조금 더 원하면 `ERROR_CORRECT_M`도 고려해 보세요.
    

```python
if save_path is None:
    save_path = "qrcode.png"
img.save(save_path)
print(f"[QR] {full_url} -> saved: {save_path}")
return save_path
```

- PNG 파일로 저장하고, **완성된 URL을 콘솔**에 찍어줍니다(디버그에 매우 유용!).
    

---

# 3) 실제로 URL이 어떻게 만들어지나? (예시)

예를 들어 `number=11`, 선택값이 아래라고 해볼게요.

```
gender=1, age=3, mbti='ISFP', fashion=3, color=8, purpose=2, category=5
```

그러면 `params`는 대략:

```
{
  perfume: 11, gender_id: 1, age_id: 3, mbti: 'ISFP',
  fashion_id: 3, pref_color_id: 8, purpose_id: 2,
  category_id: 5, top: 30, middle: 40, base: 30, lang: 'en'
}
```

`urlencode` 후 최종 URL:

```
https://perfume-mobile.vercel.app/result?
perfume=11&gender_id=1&age_id=3&mbti=ISFP&fashion_id=3&
pref_color_id=8&purpose_id=2&category_id=5&top=30&middle=40&base=30&lang=en
```

(줄바꿈 없이 한 줄입니다. 콘솔에 그대로 출력돼요.)

---

# 4) 자주 생기는 함정 & 개선 팁

1. **숫자 문자열 처리(`nz`) 강화**  
    현재 `isdigit()` 기반이라 `' 3'`, `'+3'`, `'-1'`, `'1.0'`은 0 처리됩니다.  
    더 견고하게 하려면 try/except가 낫습니다:
    

```python
def nz(x, default=0):
    try:
        # float로 한 번 열고 int로 내림(문자열 '1.0'도 OK)
        return int(float(str(x).strip()))
    except (TypeError, ValueError):
        return default
```

2. **AspectRatioMode 임포트**  
    `Qt.AspectRatioMode...`를 쓰려면:
    

```python
from PySide6.QtCore import Qt
# 또는
from PySide6 import QtCore
# self.qr_label.setPixmap(pix.scaled(100, 100, QtCore.Qt.AspectRatioMode.KeepAspectRatio))
```

3. **다이얼로그 리사이즈 시 재배치**  
    `move(width - 120, 20)`는 “그 순간”만 맞습니다.  
    윈도우 크기 변할 때도 오른쪽 위를 유지하려면 `resizeEvent`에서 다시 `move`를 호출하거나, `QHBoxLayout`/`QGridLayout`에 넣어 정렬로 고정하세요.
    
4. **QR 용량/인식률 튜닝**
    

- URL이 길면 자동으로 **QR 버전**이 커집니다.
    
- 인쇄/촬영 환경이 험하면 `ERROR_CORRECT_M`로 올리고 `box_size`를 8~10 정도 유지하면 인식률이 좋아지는 경우가 많습니다.
    

5. **프론트 파라미터 사양과 1:1 매핑 확인**
    

- `pref_color_id`, `category_id` 등 **키 이름/인덱스 기원(앱 vs 웹)**이 다르면 결과가 엇나갑니다.
    
- 지금은 `category_id`를 제대로 `scent` 선택값으로 교정하셨고, **색상/성별/나이도 프론트의 기대 인덱스**와 일치하는지 한 번 더 점검하세요.  
    (프론트에서 Blue=1, Pink=2, ... 같은 **고정 맵**이 있으면 백엔드도 그에 맞춰 변환해 주어야 합니다.)
    

6. **MBTI 비어 있을 때 처리 중복**
    

- 현재 `mbti == 'NONE'` → `""`로 비움 && dsQRcode에서 빈 값은 제거합니다.
    
- 둘 중 하나만 있어도 충분하니 유지/정리 중 하나 택하셔도 됩니다(문제는 없음).
    

---

# 5) 깔끔한 리팩토링 예시 (선택)

**파라미터 만들기**를 별도 함수로 빼두면 재사용/테스트가 쉬워져요:

```python
def build_result_params(self, perfume_number: int) -> dict:
    def nz(x, default=0):
        try:
            return int(float(str(x).strip()))
        except (TypeError, ValueError):
            return default

    mbti = self._convert_personality_to_mbti(getattr(self, 'find_per_range_num', 0))
    scent_num = getattr(self, 'find_scent_range_num', getattr(self, 'find_category_range_num', 0))

    params = {
        "perfume": perfume_number,
        "gender_id": nz(getattr(self, 'find_gender_range_num', 0)),
        "age_id": nz(getattr(self, 'find_age_range_num', 0)),
        "mbti": "" if mbti == "NONE" else mbti,
        "fashion_id": nz(getattr(self, 'find_fashion_range_num', 0)),
        "pref_color_id": nz(getattr(self, 'find_color_range_num', 0)),
        "purpose_id": nz(getattr(self, 'find_purpose_range_num', 0)),
        "category_id": nz(scent_num),
        "top": 30, "middle": 40, "base": 30,
        "lang": "en",
    }
    return params
```

그리고 본 함수는 간결하게:

```python
def setUiFindExpDlg(self, number):
    # 1) 배경
    self.ui_find_exp_dlg.label_background.setStyleSheet(dsImage.dsFindDetailImg[number])

    try:
        # 2) 파라미터
        params = self.build_result_params(number)

        # 3) QR 생성
        qr_path = dsQRcode.generate_qr_code(
            save_path=f"temp_qr_{number}.png",
            base_url="https://perfume-mobile.vercel.app/result",
            params=params,
        )

        # 4) 라벨 세팅
        if not hasattr(self, 'qr_label'):
            self.qr_label = QtWidgets.QLabel(self.ui_find_exp_dlg)
            self.qr_label.setFixedSize(100, 100)
            self.qr_label.setStyleSheet("border: none; background: transparent;")

        pix = QtGui.QPixmap(qr_path)
        self.qr_label.setPixmap(pix.scaled(100, 100, Qt.AspectRatioMode.KeepAspectRatio))
        self.qr_label.move(self.ui_find_exp_dlg.width() - 120, 20)
        self.qr_label.show()

    except Exception as e:
        print(f"QR 코드 생성 오류: {e}")
        if hasattr(self, 'qr_label'):
            self.qr_label.hide()
```

**QR 생성기** 쪽도 인식률을 약간 올리고 싶다면:

```python
qr = qrcode.QRCode(
    version=None,  # 자동으로 최적 버전
    error_correction=qrcode.constants.ERROR_CORRECT_M,  # 복원력 상향
    box_size=10,
    border=4,
)
```

---

# 6) 빠른 점검 체크리스트

-  콘솔에 찍힌 `[QR] https://...` URL을 브라우저에 붙여넣어 **결과 페이지 파라미터가 맞는지** 확인
    
-  **색상/카테고리 인덱스 맵**(앱↔웹)이 일치하는지 다시 한 번 대조
    
-  `Qt` 임포트 문제 없이 **AspectRatioMode**가 제대로 적용되는지
    
-  다이얼로그 리사이즈 시 QR 라벨 위치가 유지되는지
    
-  QR가 너무 빽빽하면 `ERROR_CORRECT_M`로 상향/버전 자동조정 사용
    

---

필요하시면, 프론트에서 기대하는 **정확한 인덱스 맵(예: pref_color_id 매핑표)**을 기준으로 백엔드에서 변환 테이블을 넣는 작업까지 바로 정리해 드릴게요.